{
    "version": 3,
    "configurePresets": [
        {
            "name": "linux-build",
            "description": "cmake preset for build on linux",
            "hidden": false,
            "generator": "Unix Makefiles",
            "binaryDir": "${workspaceFolder}/build",
            "cacheVariables": {
                "CMAKE_BUILD_TYPE" :"Debug"
                
            }
        }
    ]
}